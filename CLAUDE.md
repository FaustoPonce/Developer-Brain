# Developer Brain

**Leé `AGENTS.md` de este mismo directorio.** Es el archivo real: las reglas que aplican siempre y la tabla de ruteo tarea → nota.

Este archivo existe solo para que Claude Code, que autocarga `CLAUDE.md` y no
`AGENTS.md`, encuentre el camino. **El contenido no se duplica acá a propósito** —
dos copias divergen, y en este proyecto ya pasó.
